<div class="container m-4">
  <h1>My tasks</h1>
  <ul>
    <% @tasks.each do |task| %>
      <li><%= link_to task.title, task_path(task)  %></li>
    <% end %>
  </ul>
</div>
<div class="container">
  <%= link_to 'Add a new task', new_task_path %>
</div>